language: node_js
sudo: false
node_js:
  - "8.3"

install:
  - npm install

script:
    #Master
  - if [ $TRAVIS_BRANCH == "master" ]; then
      npm run delivery:prod;
    fi

    #Develop
  - if [ $TRAVIS_BRANCH == "develop" ]; then
      npm run delivery:prod;
    fi

# whitelist
branches:
  only:
  - master
  - develop
